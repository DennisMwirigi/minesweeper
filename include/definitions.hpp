#pragma once

levels
{
    beginner
    {
        rows = 8;
        columns = 8;
        mines = 10;
        // currentHighScore *;
    }

    intermediate
    {
        Rows = 16;
        Columns = 16;
        mines = 40;
        // currentHighScore *;
    }

    expert
    {
        rows = 16;
        columns = 30;
        mines = 99;
        // currentHighScore *;
    }
}
